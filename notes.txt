Next steps:

Run on laptop
Rework kumite match display
Freeze run divisions
Force registered people to be in a particular division
Round nomenclature
NEED TO DEAL WITH TIES IN KUMITE ROUND ROBIN
Check-in workflow
Security permissions
black belt numeric suffixes


Make venv:
python3 -m venv venv
source myvenv/bin/activate
deactivate

pip3 install django

django-admin startproject tournament

Windows download:
pip download -r ../requirements.txt --no-binary download
pip download -r ../requirements.txt --only-binary=:all: --python-version 34 --platform win_amd64 --implementation py download
pip install -r requirements.txt -no-index --find-links download

UML diagram:
  ./manage.py graph_models -o models.png kata kumite registration

Coverage:
  coverage run --source='.' manage.py test myapp
  coverage report
  coverage html
